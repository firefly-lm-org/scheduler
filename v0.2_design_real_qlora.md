# 萤火虫 v0.2 技术设计草案：真实 QLoRA 训练闭环

**目标**：firefly-client 不再产出 mock 权重，而是运行真实 QLoRA 微调（Qwen3-1.5B via Unsloth/PEFT），把真实 LoRA adapter 回传调度中心，并用 2 份真实 LoRA 验证 FedAvg 聚合协议。

**范围**：2 周内，单节点自测，不做多节点/防作弊/企业合同。

---

## 1. 现状（v0.1 mock 在哪）

`firefly-client/app/task_executor.py`：
- `simulate_training()` —— 进度条 + `asyncio.sleep`，无真实计算
- `make_result_package()` —— `numpy` 随机生成 16×64 / 64×16 矩阵写入 `lora_weights.safetensors`（**合法 safetensors 格式，仅为让服务端 FedAvg 能跑通**）
- `execute_task()` —— 领取 → 下载 → 训练 → 上传 主流程

`firefly-client/app/config.py`：
- 只有 `ClientConfig`（连接/鉴权），**没有训练配置结构** → v0.2 需新建

---

## 2. 改造清单

### 2.1 新建 `app/training/qlora_trainer.py`

真实训练入口，签名对齐现有调用：

```python
async def real_qlora_train(
    task_id: str,
    task_dir: Path,
    cfg: ClientConfig,
    tcfg: TrainingConfig,
    progress_cb=None,   # 替代原 report_progress
) -> dict:
    """
    真实 QLoRA 微调。返回训练指标 dict（loss_curve / final_loss / peak_vram_mb / steps）。
    内部用 unsloth + trl.SFTTrainer 跑 Qwen3-1.5B 4-bit。
    """
```

关键依赖（Windows 原生，需 CUDA 驱动）：
- `unsloth` + `trl` + `transformers` + `peft` + `bitsandbytes` + `datasets` + `accelerate`
- `torch` 需 CUDA 版（Windows + 无 WSL → 装 `torch` CUDA wheel，NVIDIA 驱动原生）

### 2.2 新建 `TrainingConfig`（`app/config.py` 追加）

```python
class TrainingConfig(BaseModel):
    model_name: str = "unsloth/Qwen3-1.5B"   # 或本地缓存路径
    lora_r: int = 16
    lora_alpha: int = 32
    lora_dropout: float = 0.05
    bits: int = 4                            # 4/8-bit 量化
    max_seq_length: int = 2048
    dataset: str = "firefly-zh-instruct"     # 内置/可下载 SFT 数据集
    epochs: int = 1
    max_steps: int = 200                     # 自测用小步数
    learning_rate: float = 2e-4
    output_dir: str = ""                      # 默认 task_dir/result
```

### 2.3 替换 `simulate_training()`

- 保留原进度条 UI（`Progress` + `progress_cb` 回调）
- 真实训练用 `on_step` 钩子驱动进度条
- `FIREFLY_E2E=1` 时：用极小数据集 + `max_steps=5` 跑通链路（≤ 1 分钟）

### 2.4 替换 `make_result_package()` 的权重生成

- 不再随机生成张量
- 直接从 `TrainingConfig.output_dir` 拷真实 `adapter_model.safetensors`（Unsloth/PEFT 输出）
- 同时打包：`adapter_config.json` + `training_log.json`（真实 loss_curve）+ `metrics.json`
- 保持 zip 结构兼容服务端 FedAvg：`result/lora_weights.safetensors` 或保持 `adapter_model.safetensors` 名（需同步 scheduler 聚合 worker 读取逻辑）

### 2.5 数据集策略

| 阶段 | 数据集 | 来源 |
|------|--------|------|
| v0.2 自测 | 小样本中文指令（200 条） | 内置 `app/training/data/sample_zh.jsonl` |
| v0.5 社区 | Firefly 中文指令数据集 / ShareGPT-zh | 公开 + 用户贡献 |

### 2.6 调度中心聚合 worker 对齐（`scheduler`）

当前 `scheduler` FedAvg 假设张量是固定形状（mock 的 16×64）。真实 LoRA 张量名/形状是 `base_model.model.model.layers.N.q_proj.lora_A.weight` 等。

**关键改动**：
- 聚合 worker 读取 `adapter_model.safetensors` 的真实张量名（PEFT 命名约定）
- FedAvg 按张量名逐层加权平均（已有按 key 聚合逻辑，只需对齐 key 命名）
- 聚合产物写回 `adapter_model.safetensors`，确认可用 `PeftModel.from_pretrained(base, aggregated)` load

---

## 3. 验证矩阵（v0.2 验收标准）

| # | 验证项 | 方法 | 通过标准 |
|---|--------|------|---------|
| 1 | 单节点真实训练 | `firefly-client train --e2e` | 产出 `adapter_model.safetensors`，非空，可 load |
| 2 | 权重回传 | 上传 → scheduler 存 OSS | 服务端能 `GET` 回该 safetensors |
| 3 | FedAvg 聚合 | 2 份真实 LoRA → 聚合 | 聚合权重 `PeftModel.from_pretrained` 成功，推理不报错 |
| 4 | 推理可用 | 拿聚合权重跑一条 prompt | 输出为中文连贯文本（非乱码/非随机） |
| 5 | E2E 链路 | `firefly-client run --e2e` | 领取→训练→上传→服务端聚合 全绿 |

---

## 4. 硬件前提（必须确认）

真实 QLoRA 4-bit 1.5B：
- 最低 6GB VRAM（4-bit 量化后权重 ~1GB，激活 + 优化器状态 ~4-5GB）
- 需要 NVIDIA GPU + Windows 原生 CUDA 驱动（**当前环境 Windows 11 无 WSL**）

**动手前必跑**：`firefly-client hardware` 看 GPU 检测结果。
- 有 GPU → 直接装 CUDA torch 开干
- 无 GPU → v0.2 需换带 GPU 的机器 / 云实例，或延后到 v0.5

---

## 5. 不在 v0.2 范围

- ❌ 多节点调度（1 节点自测即可）
- ❌ 防作弊（v0.5）
- ❌ 企业合同（v1.0）
- ❌ 开放原子申请（v0.5）

---

## 6. 文件改动归纳

| 文件 | 改动 |
|------|------|
| `app/training/qlora_trainer.py` | 新建，真实训练 |
| `app/training/data/sample_zh.jsonl` | 新建，自测数据集 |
| `app/config.py` | 追加 `TrainingConfig` |
| `app/task_executor.py` | `simulate_training` → `real_qlora_train`；`make_result_package` 读真实 adapter |
| `app/main.py` | CLI 增加 `train` 子命令（真实训练入口） |
| `scheduler` 聚合 worker | 对齐 PEFT 张量命名 |
| `requirements.txt` | 加 unsloth/trl/peft/bitsandbytes/datasets/accelerate |

---

## 7. 启动命令（草案）

```bash
# 1. 确认硬件
python -m firefly_client hardware

# 2. E2E 快速自测（小步数）
FIREFLY_E2E=1 python -m firefly_client train --task demo

# 3. 真实单节点训练
python -m firefly_client train --model unsloth/Qwen3-1.5B --max-steps 200

# 4. 验证聚合
python -m scheduler.scripts.verify_aggregate --a lora1.safetensors --b lora2.safetensors
```

---

**文档版本**：2026-07-25 创建，v0.2 启动前参考。
